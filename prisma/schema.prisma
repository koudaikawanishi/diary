datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Diary {
  id        Int      @id @default(autoincrement())
  content   String   @db.Text // ← 長文対応にする
  date      DateTime 
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
